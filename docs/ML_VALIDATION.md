# ML Validation

Tracked in `data/delhi/derived/science/ml_validation_results.csv` and
`ml_training_registry.csv` (regenerated by the runner script).

Current state (real data):

- hydraulic_residual MAE/RMSE/bias → NOT_COMPUTABLE (no residual target:
  no local quantitative observation exists).
- occurrence LOEO precision/recall/F1/balanced_accuracy →
  NOT_COMPUTABLE (LOEO requires ≥2 events per class; with 3 positive /
  1 negative event the exclusive negative is also the reserved control
  event; every single-event validation fold cannot contain both classes).

Policy: ML claimed useful only if held-out event-grouped metrics improve
on the physics-only baseline; training-error improvement is never
generalization. No ML result is reported for Kushak today.
